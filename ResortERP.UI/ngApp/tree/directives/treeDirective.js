app.directive('tree', function ($rootScope, $compile) {
    return {
        restrict: 'E',
        scope: false,
        templateUrl: function (scope, element) {
            var tempurl = '../../NgApp/tree/Views/' + $rootScope.language + '/tree.html';
            $rootScope.$watch(function () { return '../../NgApp/tree/Views/' + $rootScope.language + '/tree.html'; }
                , function (newVal, oldVal) {
                    if (newVal && newVal !== oldVal) {
                        element.$$element.html(newVal);
                        $compile(element.$$element)(scope);
                    }
                });
            return tempurl;
        }
    }
});