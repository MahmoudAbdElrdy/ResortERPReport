app.directive('currency', function ($rootScope, $compile) {
    return {
        restrict: 'E',
        scope: false,
        templateUrl: function (scope, element) {
            var tempurl = '../../NgApp/currency/Views/' + $rootScope.language + '/currency.html';
            $rootScope.$watch(function () { return '../../NgApp/currency/Views/' + $rootScope.language + '/currency.html'; }
                , function (newVal, oldVal) {
                    if (newVal && newVal !== oldVal) {
                        element.$$element.html(newVal);
                        $compile(element.$$element)(scope);
                    }
                });
            return tempurl;
        }
    }
})