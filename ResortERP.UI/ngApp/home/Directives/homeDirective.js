app.directive('home', function ($rootScope, $compile) {
    return {
        restrict: 'E',
        scope: false,
        templateUrl: function (scope, element) {
            var tempurl = '../../NgApp/home/Views/' + $rootScope.language + '/home.html';
            $rootScope.$watch(function () { return '../../NgApp/home/Views/' + $rootScope.language + '/home.html'; }
                , function (newVal, oldVal) {
                    if (newVal && newVal !== oldVal) {
                        element.$$element.html(newVal);
                        $compile(element.$$element)(scope);
                    }
                });
            return tempurl;
        }
    }
})